1.定窗口

    double nowValue=0;
    for(int i=0;i<k-1;i++)
    {
        nowValue+=nums[i];
    }
    int leftOut=0; 
    int rightIn=k-1;
    double maxValue=-2e32-1;//这里尤其要注意取值 如果是负数就要取负无穷
    while(rightIn<nums.length)
    {
        nowValue+=nums[rightIn];
        if(nowValue>maxValue)maxSum=nowSum;
        nowValue-=nums[leftOut];
        rightIn++;
        leftOut++;
    }
    